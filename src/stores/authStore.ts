/**
 * Auth Store — re-exports from shared/authStore.
 *
 * The canonical auth store lives in shared/authStore.ts.
 * This file exists for backward compatibility with existing imports.
 */
export { useAuthStore } from './shared/authStore'
