/**
 * Permissions endpoint — used with createService factory.
 *
 * Backend routes: /api/v1/platform/permissions
 *   GET / → permissions.index (read-only listing for role management forms)
 */
export const PERMISSIONS_ENDPOINT = '/platform/permissions'
