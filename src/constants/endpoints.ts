/**
 * Re-export endpoints from config path.
 * Several CMS services import from '@/constants/endpoints'.
 */
export { API_ENDPOINTS } from '@/config/endpoints'
export type { ApiEndpoint } from '@/config/endpoints'
